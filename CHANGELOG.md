# Changelog

Every Compresso release, in plain language. Also published at
[compresso.ai/changelog](https://compresso.ai/changelog), with an
[RSS feed](https://compresso.ai/changelog/rss.xml).

Generated from `content/changelog/` in the Compresso web repository by
`scripts/release.sh`. Do not edit this file by hand.

## 0.2.0 — 2026-09-16

Add-ons arrive, and Claude Code keeps loading tool schemas on demand behind Compresso.

### New

- **Add-ons**, a new page. Turn on Codebase Memory or Context7 and Compresso
  installs it when needed and registers it in every agent you connected, in one
  switch. Add a server of your own by name and command; Compresso registers it
  as entered without downloading, checking or updating it. Add-ons pause with
  optimization. Disconnecting an agent or successfully uninstalling removes
  owned registrations. Failed cleanup keeps the affected ownership evidence
  and exposes Manual instructions or errors for repair and retry.

### Fixed

- Claude Code's custom-host gate could disable on-demand MCP tool loading when
  ENABLE_TOOL_SEARCH was unset. Compresso now supplies `ENABLE_TOOL_SEARCH=auto`
  when the key is absent, JSON null or a blank string. On disconnect/uninstall,
  Compresso removes it only if it recorded that it wrote it and the current
  value is still the literal string `auto`. Explicit string and nonstring
  values, and later user edits, survive.

## 0.1.1 — 2026-09-04

Compresso's background helper is now optional.

### New

- **Keep agents connected after quit**, a new switch in Settings. It is on by
  default, and a small helper keeps Claude Code and Codex working while
  Compresso is closed. Turn it off and quitting Compresso restores your agents'
  original settings. Opening Compresso reconnects them.

### Improved

- Compresso repairs your agents' settings at startup if another program has
  taken its local port.

## 0.1.0 — 2026-09-03

The first version of **Compresso**.

### New

- Local optimizer for **Claude Code** and **Codex**: requests route through a
  loopback port on your Mac, redundant context is compressed away, and the
  request continues to Anthropic or OpenAI under your own credentials.
- Menu-bar savings: tokens removed and estimated cost impact, per agent, per day.
- Honest numbers: measured token counts and estimated ≈$ are reported
  separately, never blended.
- Fail-open by design: if the optimizer is down, starting, or your subscription
  lapses, requests pass straight through. Never a dead port.
- Clean uninstall: restores every config it changed and lists everything it
  will remove before it runs.
- 5-day free trial, no card. Billing by Polar as merchant of record.
